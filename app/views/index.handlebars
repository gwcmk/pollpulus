<div id="homepage" class="row">
<div class="col-md-6">
  <h1 id="welcome">Welcome to Pollpulus!</h1>
  <h3 id="welcome-desc">Pollpulus is a localized polling application that allows users to poll their community and find out where people stand on various issues. Sign up below to start participating in your local polls.<h3>
  <a type="button" class="btn btn-lg btn-green welcome-btn" aria-label="Pencil" href="/signup">
    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Sign Up
  </a>

  <a type="button" class="btn btn-lg btn-green welcome-btn" aria-label="User" href="/login">
    <span class="glyphicon glyphicon-user" aria-hidden="true"></span> Log In
  </a>
  </div>

  <div class="col-md-6">
    <h4>Today's most recent question:</h4>
    <div class="question hidden">
      {{question.question}}
    </div>

    {{#each answers}}
    <div class="answer-data hidden">
      <div class="answer">
        {{answer}}
      </div>
      <div class="votes">
        {{votes}}
      </div>
    </div>
    {{/each}}

    <div id="pie" style="height: 80vh width: 50vw; margin: 10vh auto"></div>

  </div>

</div>